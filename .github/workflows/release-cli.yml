name: Release CLI to PyPI

# Publishes only on an intentional release: pushing a tag of the form
# cli-v<version> (e.g. cli-v0.1.0). Never runs on an ordinary push/PR,
# so no build here can accidentally publish.
on:
  push:
    tags:
      - "cli-v*.*.*"

permissions:
  contents: read

jobs:
  build:
    name: Build + validate
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: "3.13"

      - name: Install SDK, then CLI, and test
        run: |
          pip install -e ./sdk
          pip install -e "./cli[dev]"
          cd cli
          ruff check aifootprint_cli tests
          mypy aifootprint_cli
          pytest

      - name: Build wheel + sdist
        working-directory: cli
        run: |
          pip install build twine
          python -m build
          twine check dist/*

      - name: Reject artifacts containing an API-key-shaped secret
        working-directory: cli
        run: |
          set -euo pipefail
          mkdir -p /tmp/cli-inspect
          for f in dist/*; do
            case "$f" in
              *.whl) python -m zipfile -e "$f" /tmp/cli-inspect ;;
              *.tar.gz) tar -xzf "$f" -C /tmp/cli-inspect ;;
            esac
          done
          if grep -rlE "afp_(live|test)_[A-Za-z0-9]+" /tmp/cli-inspect; then
            echo "::error::Built artifact contains what looks like a live AI Footprint API key. Refusing to publish."
            exit 1
          fi

      - name: Verify tag matches package version
        working-directory: cli
        run: |
          set -euo pipefail
          TAG_VERSION="${GITHUB_REF_NAME#cli-v}"
          PKG_VERSION="$(python -c 'import aifootprint_cli; print(aifootprint_cli.__version__)')"
          if [ "$TAG_VERSION" != "$PKG_VERSION" ]; then
            echo "::error::Tag $GITHUB_REF_NAME does not match aifootprint_cli.__version__=$PKG_VERSION"
            exit 1
          fi

      - uses: actions/upload-artifact@v4
        with:
          name: cli-release-dist
          path: cli/dist/

  publish:
    name: Publish to PyPI (trusted publishing)
    needs: build
    runs-on: ubuntu-latest
    environment:
      name: pypi-cli-release
      url: https://pypi.org/project/aifootprint-cli/
    permissions:
      # OIDC token for PyPI trusted publishing. No PyPI API token/secret
      # is configured or used anywhere in this workflow.
      id-token: write
      contents: read
    steps:
      - uses: actions/download-artifact@v4
        with:
          name: cli-release-dist
          path: dist

      - name: Publish to PyPI
        uses: pypa/gh-action-pypi-publish@release/v1
        with:
          packages-dir: dist/
          print-hash: true
